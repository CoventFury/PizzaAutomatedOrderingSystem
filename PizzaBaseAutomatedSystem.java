import java.awt.Font;
import java.awt.Color;
import javax.swing.JLabel;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.BorderLayout;
import javax.swing.ImageIcon;
import javax.swing.JFrame;

class PizzaBaseAutomatedSystem
{
	public static void main (String[] args)
	{
		//MainGUI gui = new MainGUI();
		PizzaBaseGUI guii = new PizzaBaseGUI();
		/*
		JFrame frame = new JFrame();
		frame.setTitle("PizzaBase Automated Ordering System");
		frame.setSize(720,720);
		frame.setResizable(false);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setVisible(true);
		frame.add(drinks);

		JFrame frame = new JFrame(); //sets the main frame
		frame.setTitle("PizzaBase Automated Ordering System"); //sets the title of the frame
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exits out of application
		frame.setResizable(false);//prevents frame from being resized
		frame.setSize(1280,720); //sets the x and y dimensions of the frame
		drinks.setVisible(true);
		frame.setVisible(true); //displays the frame;

		ImageIcon image = new ImageIcon("logo.png");//creates an ImageIcon
		frame.setIconImage(image.getImage()); //sets the frame's icon
		frame.getContentPane().setBackground(new Color(192,57,43)); //change frame's color
		*/
	}
}